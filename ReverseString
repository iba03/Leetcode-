//REVERSE STRING

void reverseString(char* s, int sSize) {
    char temp;
    int i=0,j=sSize-1;
    while(i<j)
    {
        temp=s[i];
        s[i]=s[j];
        s[j]=temp;
        i++;
        j--;
    }
    
}
//we have used C to reverse string in O(n) time complexity
